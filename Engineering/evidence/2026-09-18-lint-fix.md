# Lint and source-fix verification

## Scope and risk

Risk **R3**: public CLI compatibility, untrusted source, and replacement of user
source files. The requested scope is `--lint` and `--lint --fix`, including
stdout, `--diff`, and `--in-place`. No interpreter grammar or editor-extension
behavior is changed. Base revision: `58b8b583e90d9f808d872cacb994bc974b4ddc06`.

## Red evidence

Test-only commits retain the observed regressions before their fixes:

| Revision | Reproduced behavior |
| --- | --- |
| `0f5e979f` | Documented CLI flags rejected; lint stdout noise; ignored limits/indentation; malformed source overwritten; invalid diff format |
| `39b839e3` | Source comments, strings, grouping, constants and newer constructs lost; non-idempotent output across the TestPrograms corpus |
| `ad9d48e3` | Renaming introduces keywords or changes external member names |
| `c8a21ef2` | Extreme indentation configuration panics; checked API assertions prevent silent fallback from hiding formatter failures |
| `3ead2452` | Corrected two invalid test fixtures after confirming the parser does not accept their syntax; retained incomplete-source regression from preceding Red commit |
| `3b898ac0` | Legacy repeated-file CLI form rejected; map keys, public method parameters, and pattern captures renamed |
| `800ec957` | Inline inner block incorrectly closes outer indentation state |
| `8f8a88ed` | Diff labels name the wrong file; documented style switches ignored |
| `1f335416` | Interface action requirements incorrectly open method bodies; linter switches ignored |
| `b0b2b964` | Inline postcondition repeat leaves indentation open |
| `a77fefa1` | Unicode trailing comment whitespace is linted but not fixed |
| `e37c7812` | Real closing statements after a multiline string do not update indentation state |

Initial source-preservation run: **8 of 9 focused tests failed**, with **482
preservation failures across 206 parseable TestPrograms**. The additional
keyword/member review cases failed against the original compiled library while
parallel source edits made a normal Cargo rebuild unsuitable; their exact
command and output are in the local evidence log. Later review regressions
were each run against the then-current implementation before being fixed.

The old `test_concatenation_problematic_multiline` asserted an output line break
after `with`, which the parser rejects. Its replacement strengthens the
contract to checked, parseable output with exact source/literal preservation;
it does not enable unsupported expression syntax or hide a failing check.

## Acceptance coverage

| Requirement | Authoritative tests |
| --- | --- |
| Lint clean/findings/input-error statuses and unchanged input | `tests/lint_cli_test.rs` |
| Documented, reordered, and legacy flags; invalid combinations have no side effects | `tests/lint_cli_test.rs` |
| Pure source/diff stdout; previews do not write; second diff empty | `tests/lint_cli_test.rs` |
| Usable filename-aware patches, including spaces, CRLF and missing final newline | Real Git apply/reverse in `tests/lint_cli_test.rs`; patch structure in `tests/fixer_output_test.rs` |
| In-place refuses malformed, read-only, oversized, stale, or invalid output | CLI, output, and source-preservation tests |
| Formatting keeps literals/comments/parentheses and every parsed language form | `tests/fixer_source_preservation_test.rs`: 206 parseable programs out of 220 source files; pre-existing parse failures are outside its precondition and remain covered by parser/end-to-end suites |
| Safe renames preserve distinct bindings, public APIs, map and capture keys | Dedicated source-preservation regressions |
| Program results unchanged after real in-place formatting | `tests/fixer_runtime_test.rs`: seven programs, before/after stdout and second-diff assertions |
| Configuration and indentation agree between lint and fix | Linter units and CLI tests, including all three boolean style switches |
| Unicode, multiline strings, inline blocks, malformed layout and nesting | `src/linter/tests.rs`, source/output tests |

## Independent review and recovery

A separate review agent examined the actual CLI, linter, layout, formatter,
diff, and atomic-write implementation. It reproduced unsafe data/API renames,
ignored configuration switches, interface indentation, and Unicode whitespace
inconsistency. Each finding has a retained regression and a corresponding fix.
A 30,000-statement source probe completed in 112 ms with lint findings.

Fixing validates source before editing and reparses the output before writing.
In-place uses a same-directory temporary file, flush/sync, and rename. Stale
checks compare bounded chunks rather than reloading a potentially growing
file. Failed preparation/publication preserves the previous contents; no-op
fixes avoid replacement. This is not an interprocess editor lock, and Unix
permission-bit preservation does not guarantee ownership/custom ACL retention
or parent-directory crash durability. Diff previews and version control remain
available for reviewing/reverting intended edits.

## Validation results

Verified on Windows with Rust 1.98.1:

- `cargo build --release`: passed for the final implementation.
  Binary SHA-256: `A403A176CD1CF7A671907CCE99475ADB8037983D4211B3D9462A1F7221160E68`.
- `cargo test --all --no-fail-fast`: **2,342 passed, 0 failed, 27 existing
  ignored**, exit 0. Windows tests used `RUST_MIN_STACK=8388608` as documented.
- Focused CLI/source/output/runtime boundaries: **40 passed** (15 CLI, 18
  source-preservation, 6 output, 1 execution test covering seven programs).
- `cargo fmt --all -- --check`: passed.
- `cargo clippy --all-targets --all-features -- -D warnings`: passed.
- `cargo check --locked --manifest-path fuzz/Cargo.toml`: passed.
- `validate_docs_examples.py --force --ci --report`: **36 passed, 0 failed**.
- `scripts/run_integration_tests.ps1 -TestOnly`: passed, including all selected
  Rust integration tests and **144 WFL programs passed, 0 failed, 24 existing
  exclusions** for helpers, special test modes, or declared CI-SKIP cases.
- `scripts/run_web_tests.ps1`: **2/2 passed** (HTTP, routing, 404, headers).
  Its pre-existing TLS certificate case was skipped because OpenSSL is absent;
  the workspace's Rust TLS tests passed. No new skips or relaxed assertions
  were introduced.
- Repository hygiene static and clean-working-tree checks, plus
  `git diff --check`: passed after committing the implementation.

The first web-runner invocation stopped before running tests because the host
supplied conflicting case-variant `PATH` entries. A new hidden child process
with one effective `Path` entry ran the unchanged canonical script. Python
checks similarly used the bundled runtime and explicit Git executable directory
on the child path; the initial bare executable lookup failed before validation.
These are recorded setup failures, not retried product-test failures.

The integration runner used the same clean child-process environment. Its
Python tooling tests created `scripts/docker/__pycache__`; that generated cache
was inspected and removed after both suites finished. No source/test files
were removed or relaxed to pass hygiene checks.

Green implementation revision: **`78ec942d`**. This evidence-only follow-up
does not alter the source or test files verified above.
No remote CI, Linux execution, or release publication is claimed. Raw output
is kept only under `target/reports/lint-fix/` and
`target/test-artifacts/lint-fix/`; no generated logs are tracked.

## PR #735 review follow-up

The initial published head `480073beaedba54d4bdc2336928c5ae5b39eb20e`
passed [CI 35314161237](https://github.com/WebFirstLanguage/wfl/actions/runs/35314161237),
[Docker validation 35314161222](https://github.com/WebFirstLanguage/wfl/actions/runs/35314161222),
and [configuration lint 35314161247](https://github.com/WebFirstLanguage/wfl/actions/runs/35314161247).
Its CodeQL checks also passed. Actual job logs show 2,354 Linux workspace tests
passed (27 existing ignored), 31 extension tests passed, and both Linux and
Windows integration jobs passed 144 WFL programs and 36 documentation examples.
These results belong to that head; subsequent fixes need their own CI results.

Review findings and retained Red evidence:

- `82ac6143`: two `action_export_` unit regressions failed because exporting an
  existing action opened a false indentation block; five `single_dash` CLI
  regressions failed because `-program.wfl` was rejected. Tests cover lint,
  stdout, diff, in-place, legacy repeated paths, and a real statement following
  an export on the same physical line.
- `a92562d9`: two `version_aliases` CLI regressions failed because `-v` and `-V`
  filenames in legacy source positions printed the version banner. Standalone
  version flags remain covered by the existing help/version suite.
- `ce29fe1a`: two `atomic_write_` filesystem regressions failed because a
  formatter ignored another writer's lock or an abandoned lock. The active
  writer test coordinates a real thread with channels, checks independent-file
  progress, and verifies retry/cleanup after release. The abandoned-marker test
  requires explicit recovery and proves that a foreign lock is never removed.

Commands used for these Red observations: `cargo test --lib action_export_ --
--nocapture`, `cargo test --test lint_cli_test single_dash -- --nocapture`,
`cargo test --test lint_cli_test version_aliases -- --nocapture`, and
`cargo test --test fixer_source_preservation_test atomic_write_ -- --nocapture`.

The reported `if yes then display "first"` followed by another unclosed line
is invalid in the current parser: both lint and fix return status 2 with
`Expected 'end' after if block`. The review thread records the reproduction and
parser branches; this PR does not change grammar to accommodate the example.

In-place publication now coordinates WFL writers with an exclusive
`create_new` sibling `.wfl-fix-<hash>.lock`, held across validation, temporary
preparation, and replacement. A SHA-256 of the destination basename provides
a bounded lock filename. Contention fails without writing; an unrelated file
can still be formatted. Normal cleanup removes only the lock this operation
created. A terminated process may leave a lock; recovery requires confirming
its owner has stopped before removing the named path. This is a cooperative
protocol, not a conditional rename against arbitrary editors: unrelated
processes that ignore the lock can still race a path replacement. The user
guide documents this residual limit and recommends avoiding simultaneous edits
or previewing with `--diff`. No dependencies or CI gates were changed.

Coverage percentages remain unmeasured under the existing testing-profile gap;
the acceptance matrix and deterministic failure/recovery cases provide scenario
evidence. Useful API/helper/test documentation was added in response to the
review's docstring warning without changing the coverage configuration.

A separately instructed reviewer examined the actual follow-up diff and found
no further defects: the lock guard spans publication and cleanup, existing locks
remain untouched, and read-only/no-op paths retain their behavior. CLI alias
handling preserves both source positions and standalone version flags.

Follow-up validation on Windows / Rust 1.98.1:

- `cargo test --all --no-fail-fast`: **2,353 passed, 0 failed, 27 existing
  ignored**, with the documented Windows stack setting.
- Four focused boundary suites now contain **49 passing tests**; the linter
  units pass **30/30**, and the existing help/version suite passes **4/4**.
- Formatting, Clippy with warnings denied, locked fuzz-target compilation,
  and static repository hygiene passed.
- The release build passed after the workspace suite finished. Its first
  overlapping invocation failed before publication with Windows access denied
  while replacing `target/release/wfl.exe`; the suite includes consumers of that
  separately built executable. The original build log is retained, and building
  serially after test completion passed. No product tests were retried.
- `scripts/run_integration_tests.ps1 -TestOnly`: Rust integration tests passed;
  the WFL phase finished **143 passed, 1 failed, 24 existing exclusions**.
  `database_transaction_test.wfl` exceeded the unchanged 30-second deadline.
  This observed failure is retained for investigation, not replaced by a retry
  or counted as a passing gate. No SQLite/runtime code changed in this follow-up.
- Documentation examples passed **36/36**; the independent web runner passed
  **2/2**, with its existing OpenSSL-dependent TLS case unavailable locally.

The final PR-head CI links and end-to-end results are recorded in the PR after
publication so recording them does not create another untested head.
