// 07 - Error handling with try / when error. `error_message` holds the text.
// (Division by zero is a deterministic, cross-platform error.)
try:
    store result as 10 divided by 0
    display "result: " with result
when error:
    display "Caught an error: " with error_message
end try

display "Program continues after the error."
