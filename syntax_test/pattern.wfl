store email3 as "ke5crz@hamplex.com"


// Define named patterns
create pattern emailme:
    one or more of (any letter or digit or "._-")
    then "@"
    then one or more of (any letter or digit or "-")
    then "."
    then 2 to 6 letters
end pattern


// Simple string matching
check if email3 matches pattern emailme:
    display email3 + " is a Valid email!"
otherwise:
    display email3 + " is a Invalid email"
end check


// Custom pattern definition
create pattern greeting:
    "hello" or "hi" or "hey"
end pattern

check if "hello world" matches greeting:
    display "Found greeting!"
end check

//create a a pattern for phone numbers
create pattern phone_number:
    one or more of (digit)
    then "-"
    then one or more of (digit)
    then "-"
    then one or more of (digit)
end pattern

store phone1 as "123-456-7890"

check if phone1 matches phone_number:
    display phone1 + " is a Valid phone number!"
otherwise:
    display phone1 + " is a Invalid phone number"
end check

create pattern id_number:
   exactly 3 digits
    then "-"
    then 3 to 4 digits
    then "-"
    then exactly 3 digits
end pattern

store id1 as "1234-567-8901"

check if id1 matches id_number:
    display id1 + " is a Valid ID number!"
otherwise:
    display id1 + " is a Invalid ID number"
end check

create pattern phone:
    "("
    then exactly 3 digits
    then ")"
    then exactly 3 digits
    then "-"
    then exactly 4 digits
end pattern

store phone2 as "(123)500-7890"

check if phone2 matches phone:
    display phone2 + " is a Valid phone number!"
otherwise:
    display phone2 + " is a Invalid phone number"
end check


// var Test

store cn1 as 0

store new constant cn2 as 3.14159

store new constant cn3 as "azusa"

display cn2
display cn3

change cn1 to 1
add 5 to cn1
subtract 2 from cn1
multiply cn1 by 3
divide cn1 by 2

display "Final count is: " + cn1


create list shopping:
    add "milk"
    add "bread"
    add "eggs"
end list

add "butter" to shopping 
remove "eggs" from shopping

display "Shopping list contains: " + shopping

clear shopping list

// create map
create map settings:
    theme is "dark"
    volume is 75
    notifications is true
end map


// create date and time
create date today
create time now
display "Date and time created successfully!"

// Compile patterns for performance (future feature)
// compile pattern "email" as email_validator

// Test that the pattern was parsed successfully  
display "Pattern definition parsed successfully!"
